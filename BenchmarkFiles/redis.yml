imports:
  - ./BenchmarkFiles/LoadGenerators/bombardier.yml

jobs:
  server:
    source:
      localFolder: ../
      project: DbBenchmark/DbBenchmark.csproj
      selfContained: false
    dotnetTrace: true
    options:
      collectCounters: true
      traceOutput: redis_trace

scenarios:
  bomb:
    application:
      job: server
    load:
      job: bombardier
      variables:
        serverPort: 5000
        path: /Benchmark/Redis

profiles:
  local:
    variables:
      serverAddress: localhost
    jobs:
      application:
        endpoints:
          - http://localhost:5010
      load:
        endpoints:
          - http://localhost:5010